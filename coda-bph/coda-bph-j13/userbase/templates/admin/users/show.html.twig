{% extends "layout.html.twig" %}

{% block header %}
    {% include "partials/_header-admin.html.twig" %}
{% endblock %}

{% block main %}
    <main class="container py-5">
        <h1>Détails utilisateur</h1>
        <div class="card mt-3" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{{ user_inspect['firstName'] }} {{ user_inspect['lastName'] }}</h5>
                <h6 class="card-subtitle mb-2 text-body-secondary">{{ user_inspect['email'] }}</h6>
                <p class="card-text">{{ user_inspect['role'] }}</p>
                <a href="index.php?route=update&id={{ user_inspect['id'] }}" class="card-link">Modifier</a>
                <a href="index.php?route=delete&id={{ user_inspect['id'] }}" class="card-link">Supprimer</a>
            </div>
        </div>
    </main>
{% endblock %}